extern crate rand;
extern crate sdl2;
mod font;
mod modules;
mod processor;

const CHIP8_WIDTH: usize = 64;
const CHIP8_HEIGHT: usize = 32;
const CHIP8_MEMORY: usize = 4096;

fn main() {
    println!("Hello, world!");
}
